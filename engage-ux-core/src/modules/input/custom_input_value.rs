/// Value type for custom input data
#[derive(serde::Serialize, serde::Deserialize, Debug, Clone, PartialEq)]
pub enum CustomInputValue {
	// Placeholder for removed CustomInputValue. Keep this stub briefly while
	// migrating consumers; remove entirely in follow-up.
    
	#[allow(dead_code)]
	pub(crate) enum __CustomInputValueRemoved {}
}
