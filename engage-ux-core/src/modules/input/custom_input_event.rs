use crate::modules::input::custom_input_value::CustomInputValue;
// This file used to define `CustomInputEvent`.
// That type has been removed as part of the provider-specific event migration.
// Kept as an empty stub for a short transitional period to simplify edits
// to other crates. Remove this file entirely in a follow-up when consumers
// have migrated.
// This module was removed during migration away from generic custom input
// events. Keep an empty module here temporarily so downstream edits
// referencing the file can be updated incrementally. Remove this file
// completely after consumers have migrated.

#![allow(dead_code)]

/// Placeholder struct; not part of public API.
pub(crate) struct __CustomInputEventRemoved;

